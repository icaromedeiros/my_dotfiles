# MacOS defaults and packages

Here we include installing `brew.sh`, installing packages, and MacOS defaults.

1. Install brew
2. Run `sh brew_pkgs.sh`
3. Run `sudo sh macos/defaults.sh` to get useful defaults for development.